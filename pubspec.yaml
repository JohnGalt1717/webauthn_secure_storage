name: webauthn_secure_storage_workspace
description: Tooling-only workspace package for repository-level analysis settings.
publish_to: 'none'
version: 0.0.0

environment:
  sdk: '>=3.11.0 <4.0.0'

dev_dependencies:
  flutter_lints: '>=5.0.0 <7.0.0'
